This is a small project to convert data separated by | to XML

To use the parser call the main() with first the name of the file to be parsed and optionally the name of the
XML file you want the output in. 